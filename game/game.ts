// import the game
import agar = require("./agar")

/**
 * Main function
 */
function main() {
    var canvas = document.getElementById("canvas") as HTMLCanvasElement
    var context = canvas.getContext("2d")
}


window.addEventListener("load", () => main())